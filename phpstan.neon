includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5 # Relaxed for FMJ fork — bot AI uses dynamic arrays
    paths:
        - app
        - legacy
        - tests
